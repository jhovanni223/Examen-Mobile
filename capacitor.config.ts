import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Examen_mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
